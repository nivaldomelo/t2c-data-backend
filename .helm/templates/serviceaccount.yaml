apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.appName }}
  labels:
{{ include "t2c.labels" . | indent 4 }}
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
    helm.sh/hook-weight: "-10"
    helm.sh/hook-delete-policy: before-hook-creation
# Sem IRSA (eks.amazonaws.com/role-arn) — padrão atual Turn2C usa chaves AWS via Secret.
